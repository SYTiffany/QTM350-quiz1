cd "C:\Users\lucas\Documents"
git clone https://github.com/lucasgoldfein/qtm350-quiz01
cd qtm350-quiz01
touch commands.txt
mkdir reports
cd reports
touch summary.md
echo "This document provides a summary of the project updates." >> summary.md
cd ../
git add .
git status
git commit -m "Add reports directory"
cd reports
touch file{1..3}.txt
cd ../
mv data/raw-data.csv data/input-data.csv
mkdir scripts/backup && cp reports/* scripts/backup
rm docs/documentation.md
git status
git add .
git commit -m "Update project files"
touch .gitignore
echo "temp/" >> .gitignore
echo "dataset0?.csv" >> .gitignore
cat .gitignore
git add .gitignore && git commit -m "Add .gitignore file"

# Fixing it not being on a branch:

git checkout 4258465f905c91b262f8891fc3eda1ad7f30041f
git branch feature-update
git checkout feature-update

# *copy all previous commands into the branch*

git checkout main
git merge feature-update

